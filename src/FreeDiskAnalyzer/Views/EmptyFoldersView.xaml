<UserControl x:Class="FreeDiskAnalyzer.Views.EmptyFoldersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:res="clr-namespace:FreeDiskAnalyzer.Resources">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{x:Static res:Strings.EmptyFolders_Title}" Style="{StaticResource TitleTextStyle}"/>
            <TextBlock Text="{x:Static res:Strings.EmptyFolders_Subtitle}"
                       Style="{StaticResource SubtitleTextStyle}" Margin="0,4,0,8"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <Ellipse Width="10" Height="10" Margin="0,0,6,0" VerticalAlignment="Center" Fill="#16A34A"/>
                <TextBlock Text="{x:Static res:Strings.Risk_Low}" Style="{StaticResource CaptionTextStyle}"/>
            </StackPanel>
            <Button Content="{x:Static res:Strings.EmptyFolders_DeleteAll}" HorizontalAlignment="Left" Margin="0,0,0,20"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding DeleteAllFoldersCommand}"
                    Visibility="{Binding HasScanResult, Converter={StaticResource BoolToVis}}"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <TextBlock Text="{x:Static res:Strings.EmptyFolders_NoScan}" Style="{StaticResource SubtitleTextStyle}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="{Binding HasScanResult, Converter={StaticResource InverseBoolToVis}}"/>

            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          Visibility="{Binding HasScanResult, Converter={StaticResource BoolToVis}}">
                <StackPanel>
                    <TextBlock Text="{x:Static res:Strings.EmptyFolders_NoneFound}"
                               Margin="0,24,0,0" HorizontalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource SubtitleTextStyle}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Folders.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <ItemsControl ItemsSource="{Binding Folders}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,10" Padding="16">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource TextPrimaryBrush}"/>
                                            <TextBlock Text="{Binding FullPath}" Style="{StaticResource CaptionTextStyle}"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                            <Button Content="{x:Static res:Strings.LargeFiles_Open}" Margin="0,0,8,0"
                                                    Style="{StaticResource OutlineButtonStyle}" Padding="12,6" FontSize="12"
                                                    Command="{Binding DataContext.OpenFolderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"/>
                                            <Button Content="{x:Static res:Strings.LargeFiles_ShowInExplorer}" Margin="0,0,8,0"
                                                    Style="{StaticResource OutlineButtonStyle}" Padding="12,6" FontSize="12"
                                                    Command="{Binding DataContext.ShowInExplorerCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"/>
                                            <Button Content="{x:Static res:Strings.EmptyFolders_Delete}"
                                                    Style="{StaticResource OutlineButtonStyle}" Padding="12,6" FontSize="12"
                                                    Command="{Binding DataContext.DeleteFolderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
