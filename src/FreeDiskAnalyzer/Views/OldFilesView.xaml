<UserControl x:Class="FreeDiskAnalyzer.Views.OldFilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:res="clr-namespace:FreeDiskAnalyzer.Resources">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{x:Static res:Strings.OldFiles_Title}" Style="{StaticResource TitleTextStyle}"/>
            <TextBlock Text="{x:Static res:Strings.OldFiles_Subtitle}"
                       Style="{StaticResource SubtitleTextStyle}" Margin="0,4,0,20"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <TextBlock Text="{x:Static res:Strings.OldFiles_NoScan}" Style="{StaticResource SubtitleTextStyle}"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="{Binding HasScanResult, Converter={StaticResource InverseBoolToVis}}"/>

            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          Visibility="{Binding HasScanResult, Converter={StaticResource BoolToVis}}">
                <ItemsControl ItemsSource="{Binding Files}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,10" Padding="16">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        <TextBlock Text="{Binding FullPath}" Style="{StaticResource CaptionTextStyle}"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                        <StackPanel Margin="0,0,16,0">
                                            <TextBlock Text="{x:Static res:Strings.OldFiles_LastModified}"
                                                       Style="{StaticResource CaptionTextStyle}" HorizontalAlignment="Right"/>
                                            <TextBlock Text="{Binding LastWriteTimeUtc, StringFormat=d}" FontWeight="SemiBold"
                                                       HorizontalAlignment="Right"
                                                       Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        </StackPanel>
                                        <Button Content="{x:Static res:Strings.LargeFiles_Open}" Margin="0,0,8,0"
                                                Style="{StaticResource OutlineButtonStyle}" Padding="12,6" FontSize="12"
                                                Command="{Binding DataContext.OpenFileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>
                                        <Button Content="{x:Static res:Strings.LargeFiles_ShowInExplorer}"
                                                Style="{StaticResource OutlineButtonStyle}" Padding="12,6" FontSize="12"
                                                Command="{Binding DataContext.ShowInExplorerCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
