<UserControl x:Class="FreeDiskAnalyzer.Views.DuplicatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:res="clr-namespace:FreeDiskAnalyzer.Resources">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{x:Static res:Strings.Duplicates_Title}" Style="{StaticResource TitleTextStyle}"/>
            <TextBlock Text="{x:Static res:Strings.Duplicates_Subtitle}"
                       Style="{StaticResource SubtitleTextStyle}" Margin="0,4,0,8"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <Ellipse Width="10" Height="10" Margin="0,0,6,0" VerticalAlignment="Center" Fill="#16A34A"/>
                <TextBlock Text="{x:Static res:Strings.Risk_Low}" Style="{StaticResource CaptionTextStyle}"/>
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="{x:Static res:Strings.Duplicates_Drive}" Style="{StaticResource CaptionTextStyle}" Margin="0,0,0,6"/>
                <ComboBox ItemsSource="{Binding Drives}" SelectedItem="{Binding SelectedDrive}" Width="320" HorizontalAlignment="Left"
                          Style="{StaticResource AppComboBoxStyle}"
                          DisplayMemberPath="DisplayName"/>

                <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                    <Button Content="{x:Static res:Strings.Duplicates_StartScan}" Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding StartScanCommand}" Margin="0,0,12,0"/>
                    <Button Content="{x:Static res:Strings.Duplicates_Cancel}" Command="{Binding CancelScanCommand}"
                            Style="{StaticResource OutlineButtonStyle}"
                            Visibility="{Binding IsScanning, Converter={StaticResource BoolToVis}}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,16,0,0"
                            Visibility="{Binding IsScanning, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="{x:Static res:Strings.Duplicates_Scanning}" Style="{StaticResource CaptionTextStyle}" Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding CurrentPath}" Style="{StaticResource CaptionTextStyle}" TextTrimming="CharacterEllipsis"/>
                </StackPanel>

                <Button Content="{x:Static res:Strings.Duplicates_DeleteAll}" Margin="0,16,0,0" HorizontalAlignment="Left"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding DeleteAllGroupsCommand}"
                        Visibility="{Binding HasRunOnce, Converter={StaticResource BoolToVis}}"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <TextBlock Text="{x:Static res:Strings.Duplicates_NoneYet}"
                       HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource SubtitleTextStyle}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasRunOnce}" Value="False">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <TextBlock Text="{x:Static res:Strings.Duplicates_NoneFound}"
                       HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource SubtitleTextStyle}">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding HasRunOnce}" Value="True"/>
                                    <Condition Binding="{Binding Results.Count}" Value="0"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible"/>
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Results}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,10" Padding="16">
                                <StackPanel>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}">
                                            <Run Text="{Binding FilePaths.Count, Mode=OneWay}"/>
                                            <Run Text="{x:Static res:Strings.Duplicates_Copies}"/>
                                        </TextBlock>
                                        <TextBlock Grid.Column="1" FontWeight="SemiBold" Foreground="{DynamicResource AccentBrush}">
                                            <Run Text="{x:Static res:Strings.Duplicates_WastedSpace}"/>
                                            <Run Text=": "/>
                                            <Run Text="{Binding WastedBytes, Converter={StaticResource ByteSizeConverter}, Mode=OneWay}"/>
                                        </TextBlock>
                                    </Grid>
                                    <ItemsControl ItemsSource="{Binding FilePaths}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,2">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{Binding}" Style="{StaticResource CaptionTextStyle}"
                                                               TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                                    <Button Grid.Column="1" Content="{x:Static res:Strings.LargeFiles_ShowInExplorer}"
                                                            Style="{StaticResource OutlineButtonStyle}" Padding="10,4" FontSize="11"
                                                            Command="{Binding DataContext.ShowInExplorerCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding}"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <Button Content="{x:Static res:Strings.Duplicates_Delete}"
                                            Style="{StaticResource OutlineButtonStyle}"
                                            Command="{Binding DataContext.DeleteGroupCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            HorizontalAlignment="Left" Margin="0,10,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
